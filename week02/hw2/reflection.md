# HW2 Reflection: Data Governance for Audio Pipelines

My pipeline generates synthetic speech, saves it as MP3 files, and then transcribes one of those files back into text. In my run, the script stored generated audio inside `audio-output/voice_nova_sample.mp3` and `audio-output/voice_alloy_sample.mp3`, wrote API metadata to `api-call-log.csv`, and saved the overall execution details to `last-run-summary.json`. Before submission, I should update the bracketed placeholders below with the exact values from my own run, such as the measured latency, transcript text, file size, and pipeline cost. Using those concrete run artifacts matters because governance decisions depend on the real content and sensitivity of the data that my app actually handled.

For consent, if this pipeline processed real user audio instead of synthetic audio, I would need an explicit consent screen before recording or uploading any speech. The screen should clearly say that the app will upload audio to Gemini for transcription, may temporarily store files such as `[replace with your file name]`, and will generate logs containing timestamps, model IDs, and cost data. It should appear before the microphone starts recording or before a file upload begins. The user should have to actively press something like “I agree to audio processing.” Revocation should also be possible after the fact. For example, a settings page should let the user delete stored recordings and transcripts, and it should explain that already-processed third-party API logs may not be fully retractable once sent, although local copies can still be removed.

Retention should depend on context. In a study app that generates audio lessons, I would keep files only as long as needed for the learner to replay them, maybe `[replace with policy]`, because the content is usually lower risk and can often be regenerated. In a customer service transcription tool, retention may need to match support audit requirements, but it should still be limited and documented, such as storing transcripts longer than raw audio and deleting recordings earlier. In a medical intake form, retention should be much stricter because both the spoken content and the voice itself may reveal highly sensitive health information. In that case, raw audio should be minimized, access should be tightly controlled, and deletion schedules should follow legal and institutional policy rather than convenience.

Audio also carries PII that plain text often does not. Even if my transcript came back close to the original text with `[replace with accuracy]%` word overlap, the audio still exposes much more than words. A person’s voice can function like a biometric identifier. Accent, dialect, and language can reveal regional or ethnic background. Tone and pacing may suggest stress, illness, or emotional state. Background sounds can reveal location, family members, traffic, television programs, or workplace context. File metadata may also expose device or recording details. In my pipeline, even a short file such as `[replace with file name]` could reveal more identity information than its transcript alone.

For my capstone, governance depends on whether we use audio directly. If our team does not currently use audio, the immediate governance burden is lower because we avoid storing voice biometrics and environmental recordings. But if we added audio later, we would need to redesign consent, retention, access control, deletion workflows, and disclosure language from the start. My takeaway from this homework is that the governance problem is not just “what words were spoken,” but also “what hidden signals travel with the recording.” That is why the script’s concrete outputs, including `[replace with transcript snippet]`, `[replace with duration]` seconds of audio, and `[replace with total cost]` total pipeline cost, should be documented carefully and handled with a clear data policy.
